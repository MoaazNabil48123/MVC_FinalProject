@model Product
@{
    ViewData["Title"] = "Details";
    Layout = "_MyLayout";
}

@section Links{
    <link rel="stylesheet" href="~/css/css_Shop/style.css" />
    <link rel="stylesheet" href="~/css/css_Shop/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/css_Shop/fontawesome-all.min.css" />

   <link rel="stylesheet" href="~/css/css_Shop/owl.carousel.min.css">
    <link rel="stylesheet" href="~/css/css_Shop/flaticon.css">
    <link rel="stylesheet" href="~/css/css_Shop/slicknav.css">
    <link rel="stylesheet" href="~/css/css_Shop/animate.min.css">
    <link rel="stylesheet" href="~/css/css_Shop/magnific-popup.css">
    <link rel="stylesheet" href="~/css/css_Shop/themify-icons.css">
    <link rel="stylesheet" href="~/css/css_Shop/slick.css">
    <link rel="stylesheet" href="~/css/css_Shop/nice-select.css">

}
    <!-- Hero Area Start-->
   <div class="slider-area ">
        <div class="single-slider slider-height2 d-flex align-items-center">
            <div class="container">
                <div class="row">
                    <div class="col-xl-12">
                        <div class="hero-cap text-center">
                            <h2>Watch Shop</h2>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Hero Area End-->


    <!--================Single Product Area =================-->
    <div class="product_image_area">
        <div class="container">
            <div class="row justify-content-center">
            <div class="col-lg-12" style="height:auto">

                <img src="@Model.Image" alt="Loading "  />
                
                </div>
                <div class="col-lg-10">
                    <div class="single_product_text text-center">
                        <h3>
                           @Model.Name
                        </h3>
                        <p>@Model.Description                        </p>
                    <p id="modelId"  hidden>@Model.Id</p>
                    @{
                        int i = 0;
                    }
                    @foreach (Variation variation in ViewBag.variationWithOptions)
                    {
                        i++;
                        <div class="form-check" id="var">
                        <label class="form-check-label" for="flexRadioDefault1">

                            @variation.Name


                        </label>
                        <br/>
                            @foreach (VariationOptions x in variation.VariationOptions)
                        {

                                <input class="form-check-input" type="radio" name="flexRadioDefault+@i" id="flexRadioDefault1" value="@x.Id"  onclick="GetPrice()" >
                        <label  class="form-check-label" for="flexRadioDefault1">
                          
                           @x.Value
                           
                        </label>
                        <br/>
                        }
                    </div>
                            }

                  <div class="single_product_text text-center">
                      <div id="itemID" hidden></div>
                      <div >Price :</div>
                        <div id="price" ></div>

                        @*    @foreach (ProductItem productItem in ViewBag.selectedproductItems) {
                                <h3 >
                                    @productItem.ProductConfigurations
                            </h3> }*@
                       
                        </div>
@*                    <div class="dropdown">
                         @foreach (Variation variation in ViewBag.variationWithOptions)
                        {
                            i++;

                                        <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                @variation.Name
                        </button>
                                <ul class="dropdown-menu">
                            @foreach (VariationOptions x in variation.VariationOptions)
                            {           
                                    <li class="list-group-item">
                                        <input class="form-check-input" type="radio" name="flexRadioDefault+@i" id="flexRadioDefault1" onclick="GetPrice(@x.Id)" value="@x.Id">
                                        <label class="form-check-label" for="flexRadioDefault1">

                                            @x.Value

                                        </label>
                                    </li>

                           
                        }
                       
                        </ul>}
                    </div>
*@
@*                    @foreach (Variation variation in ViewBag.variationWithOptions)
                    {
                        <div>
                            <label> @variation.Name </label>
                            <select name="CourseID" id="CourseID" class="form-control">
                            @foreach (VariationOptions x in variation.VariationOptions)
                            {
                               
                                    <option> @x.Value </option>}
                            </select>
                        </div>
                            <br />
                          
                      
                    }
*@
                  




@*                    <table class="table table-hover table-bordered">
                        <tr>
                            <th>ID</th>
                            <th>Name</th>
                            <th class="text-center">Price</th>
                            <th class="text-center">Quantity</th>

                        </tr>

                        @{
                            int i = 1;
                        }
                        @foreach (var item in Model.ProductItems)
                        {
                            <tr>
                                <td>@i</td>
                                <td>@item.SKU</td>
                                <td>@item.Price</td>
                                <td>@item.StockQty</td>
                            


                            </tr>
                            ++i;
                        }
                    </table>*@








                    <div class="card_area ">
                        <div class="row pl-3 mb-6 justify-content-md-center align-items-center">
                            <div class="col-md-3">
                                <div class="input-group mb-3">
                                    <span class="input-group-text bg-primary text-white border-0 fw-semibold"
                                          id="inputGroup-sizing-default">
                                        Count
                                    </span>
                                    <input type="number" value="1" class="form-control text-end" min="0" id="counter"
                                           aria-label="Sizing example input" aria-describedby="inputGroup-sizing-default" />

                                </div>
                            </div>
                        </div>
                        <div class="add_to_cart justify-content-md-center align-items-center">
                                <a href="#" class="btn_3">add to cart</a>
                            <a asp-action="Index?@Model.CategoryId" class="btn_3">
                                Back to home
                            </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
















@section Scripts{

    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
   
    <script src="~/lib/jquery/dist/jquery.min.js"></script>



    @*<script>
       
        function GetPrice()
        {
            let elements = document.querySelectorAll("#flexRadioDefault1");
            console.log(elements);
            let selected = [];
            for (const element of elements) {
                if(element.checked)
                {
                      selected.push(element.value);  
                }
            }
            console.log(selected);


           
        }
    </script>*@
    <script>

        function GetPrice() {
            let elements = document.querySelectorAll("#flexRadioDefault1");
            console.log(elements);
         
            let selected = [];
            for (const element of elements) {
                if (element.checked) {
                    selected.push(element.value);
                }
            }
            console.log(selected)
            let nos = document.querySelectorAll("#var").length
            console.log(nos)
            if(selected.length==nos){
                GetPriceAjax(selected);
            }
           
           return selected;
           

        }
        
    </script>
    <script>

        function GetPriceAjax(selected) {
            ;

            //var selected = GetPrice();

            var productId = parseInt(document.getElementById("modelId").innerText)
            $.ajax({
                url: "/ProductItem/GetProductItem",
                type: "POST",
                data: {
                    variationOptions: selected,
                    productId: productId
                },
                success: function (response) {
                    console.log(response);
                    document.getElementById("price").innerText = response[0];
                    document.getElementById("counter").max = response[1];
                },
                error: function (xhr, textStatus, errorThrown) {
                    console.log(xhr.responseText);
                }
            });
        }
        
    </script>


   @* <script>
       
        function GetPriceAjax()
        {
            let nos = document.querySelectorAll("#var").length
            console.log(nos);
            
            var selected=GetPrice();

            var productId = parseInt(document.getElementById("modelId").innerText)
            $.ajax
                ({
                    url: "/ProductItem/GetProductItem?productId=" + productId,
                    success: function (response) {
                        //var List = document.getElementById("price");
                        //console.log(List)
                        //List.innerHTML = "";
                        console.log(response)
                        let selectedItems = [];
                        for (const result of response) {
                            for (const x in result.ProductConfigurations) {
                                let testItems = []
                                testItems.push(x.VariationOptionsId);
                                if (JSON.stringify(testItems) === JSON.stringify(selected)) {
                                    return result.Price;
                                }
                            }
                        }
                        //console.log(result.Price);


                    }


                })
        }
        var pricrNeeded= GetPriceAjax();
        var price = document.getElementById("#price");
        console.log(price);
        price.innerHTML = pricrNeeded;
    </script>*@
@*    <script>
        GetPrice2();
        function GetPrice2() 
        {
            let btn = document.querySelector('#flexRadioDefault1');
            console.log(btn);
            const radioButtons = document.querySelectorAll('#flexRadioDefault1');
            let selectedSize = [];
            let n = 0;
           
                for (const radioButton of radioButtons) {
               
                radioButton.addEventListener("click", () => 
                {
                    if (radioButton.checked)
                    {
                        selectedSize[n] = radioButton.value;
                        n++;
                        
                    }

                })
                // show the output:
               // output.innerText = selectedSize ? `You selected ${selectedSize}` : `You haven't selected any size`;
               console.log(selectedSize)
            };
        }
    </script>
*@

@*
    <script src="~/js/js2/vendor/modernizr-3.5.0.min.js"></script>
*@    <!-- Jquery, Popper, Bootstrap -->
   @* <script src="~/js/js2/vendor/jquery-1.12.4.min.js"></script>
    <script src="~/js/js2/popper.min.js"></script>*@
@*    <script src="~/js/js2/bootstrap.min.js"></script>
*@    <!-- Jquery Mobile Menu -->
@*    <script src="~/js/js2/jquery.slicknav.min.js"></script>
*@
    <!-- Jquery Slick , Owl-Carousel Plugins -->
   @* <script src="~/js/js2/owl.carousel.min.js"></script>
    <script src="~/js/js2/slick.min.js"></script>*@

    <!-- One Page, Animated-HeadLin -->
  @*  <script src="~/js/js2/wow.min.js"></script>
    <script src="~/js/js2/animated.headline.js"></script>
    <script src="~/js/js2/jquery.magnific-popup.js"></script>*@

    <!-- Scroll up, nice-select, sticky -->
  @*  <script src="~/js/js2/jquery.scrollUp.min.js"></script>
    <script src="~/js/js2/jquery.nice-select.min.js"></script>
    <script src="~/js/js2/jquery.sticky.js"></script>*@

    <!-- contact js -->
   @* <script src="~/js/js2/contact.js"></script>
    <script src="~/js/js2/jquery.form.js"></script>
    <script src="~/js/js2/jquery.validate.min.js"></script>
    <script src="~/js/js2/mail-script.js"></script>
    <script src="~/js/js2/jquery.ajaxchimp.min.js"></script>*@

    <!-- Jquery Plugins, main Jquery -->
 @*   <script src="~/js/js2/plugins.js"></script>
    <script src="~/js/js2/js/main.js"></script>*@

    <!-- swiper js -->
  @*  <script src="~/js/js2/js/swiper.min.js"></script>
    <!-- swiper js -->
    <script src="~/js/js2/mixitup.min.js"></script>
    <script src="~/js/js2/jquery.counterup.min.js"></script>
    <script src="~/js/js2/waypoints.min.js"></script>*@
}